/**
 * @customElement
 * @polymer
 */

class StreamPurchases extends Polymer.Element {
  static get is() { return 'stream-purchases'; }
  static get properties() {
    return {
    };
  }
}
window.customElements.define(StreamPurchases.is, StreamPurchases);