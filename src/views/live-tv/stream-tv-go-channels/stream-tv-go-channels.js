/**
 * @customElement
 * @polymer
 */

class StreamTvGoChannels extends Polymer.Element {
  static get is() { return 'stream-tv-go-channels'; }
  static get properties() {
    return {
    };
  }
}
window.customElements.define(StreamTvGoChannels.is, StreamTvGoChannels);