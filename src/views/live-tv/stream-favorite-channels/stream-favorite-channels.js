/**
 * @customElement
 * @polymer
 */

class StreamFavoriteChannels extends Polymer.Element {
  static get is() { return 'stream-favorite-channels'; }
  static get properties() {
    return {
    };
  }
}
window.customElements.define(StreamFavoriteChannels.is, StreamFavoriteChannels);