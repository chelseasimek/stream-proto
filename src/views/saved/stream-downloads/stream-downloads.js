/**
 * @customElement
 * @polymer
 */

class StreamDownloads extends Polymer.Element {
  static get is() { return 'stream-downloads'; }
  static get properties() {
    return {
    };
  }
}
window.customElements.define(StreamDownloads.is, StreamDownloads);