/**
 * @customElement
 * @polymer
 */

class StreamAllChannels extends Polymer.Element {
  static get is() { return 'stream-all-channels'; }
  static get properties() {
    return {
    };
  }
}
window.customElements.define(StreamAllChannels.is, StreamAllChannels);