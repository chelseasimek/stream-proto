/**
 * @customElement
 * @polymer
 */

class StreamForYou extends Polymer.Element {
  static get is() { return 'stream-for-you'; }
  static get properties() {
    return {
    };
  }
}
window.customElements.define(StreamForYou.is, StreamForYou);